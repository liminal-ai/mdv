# Epic 6: Hardening and Electron Wrapper

This epic defines the complete requirements for performance hardening under
real-world conditions and the optional Electron desktop wrapper. It serves as
the source of truth for the Tech Lead's design work.

---

## User Profile

**Primary User:** Technical agentic user who has been using MD Viewer daily as a browser-based app. They work with directories containing hundreds of markdown files, keep many tabs open across sessions, and occasionally encounter large documents generated by AI agents. Some users want to double-click a `.md` file in Finder and have it open in a dedicated app window rather than a browser tab.
**Context:** The app works. The user now hits real-world edge cases: deep trees, large files, many tabs, slow filesystems, and occasional symlink tangles. They want reliability at scale. Desktop users want native-feeling integration — file associations, a dock icon, and a quit flow that matches macOS conventions.
**Mental Model:** "It should handle my real directories without choking. And I'd like it to feel like a real app, not a browser tab."
**Key Constraint:** Hardening must not regress existing behavior. The Electron wrapper must load the same Fastify-served web app — no forking the rendering pipeline or duplicating server logic. Must work on macOS without Developer ID signing or notarization. Ad-hoc signing is required for Apple Silicon compatibility (see Amendment 1).

---

## Feature Overview

After this epic ships, the app handles real-world scale without degradation:
10,000-line documents render and scroll smoothly, file trees with 1,000+
markdown files load without freezing, and 20+ open tabs don't slow down
switching or rendering. Startup is optimized.

Optionally, the user can install the app as a macOS desktop application via a
one-command install script. The Electron wrapper runs the same Fastify server
in-process and opens a BrowserWindow pointed at localhost. Double-clicking a
`.md` file in Finder opens it in the app. The quit flow uses the Electron-specific
dirty-tabs modal specced in Epic 5 (AC-5.3a–d). Tab state persists across
restarts.

---

## Scope

### In Scope

Two concerns that together produce the M5 release candidate:

**Performance hardening:**
- Large file handling: documents over 10,000 lines render, scroll, and edit without freezing
- Deep tree handling: file trees with 1,000+ markdown files load and expand responsively
- Many-tab handling: 20+ open tabs do not degrade switching, rendering, or memory
- Startup time optimization: app is ready to use within 3 seconds
- Filesystem edge cases: permissions, symlinks (loops, broken targets), missing files, network-mounted filesystems
- Mermaid render caching: bounded cache of rendered SVGs to avoid re-rendering unchanged diagrams on tab switch, mode switch, or theme switch

**Electron wrapper:**
- Thin Electron shell that runs Fastify in-process and loads the web app in a BrowserWindow
- Native macOS menu bar mirroring the web app's menu structure (File, Export, View)
- File associations: register as `.md`/`.markdown` handler on macOS
- `open-file` event handling: double-clicking a `.md` file or dragging it onto the dock icon opens it in the app
- Single-instance lock: second launch routes files to the existing window
- Electron-specific quit flow: the custom dirty-tabs modal from Epic 5 AC-5.3a–d (Save All and Quit, Discard All and Quit, Cancel)
- Tab state persistence across app restarts
- Packaging: stable bundle ID, ad-hoc code signing, install to `~/Applications`
- One-command local install script

### Out of Scope

- Code signing and notarization (documented as future if distribution requires it)
- App Store distribution
- Auto-update mechanism (documented as future direction)
- Windows or Linux Electron packaging (macOS first; the web app already works cross-platform)
- Windows or Linux file associations
- Tab drag-to-reorder (deferred from Epic 2, remains deferred)
- Search within documents or across files (post-v1)
- New rendering features or editor capabilities
- New Fastify API endpoints (existing endpoints may have their request/response shapes extended, consistent with how Epics 2–5 extended `SessionState`)
- Modifications to the rendering pipeline
- CLI export mode (the POC had this; it is not part of the v1 Electron wrapper)

### Assumptions

| ID | Assumption | Status | Owner | Notes |
|----|------------|--------|-------|-------|
| A1 | Epics 1–5 are complete: the full Fastify-based web app is functional with browsing, viewing, rich content, export, and editing | Unvalidated | Dev team | Epic 6 wraps and hardens the existing app |
| A2 | Electron runs Fastify in-process in the main process. The server binds to localhost on a dynamic port. The BrowserWindow loads `http://localhost:{port}`. No separate child process for the server. | Unvalidated | Tech Lead | In-process avoids ~50MB memory overhead of a child process and simplifies lifecycle management |
| A3 | The Electron wrapper does not duplicate server logic — file operations, session persistence, export, and file watching all use the existing Fastify API endpoints. The only IPC needed is for capabilities the browser cannot provide: native menu events, `open-file` from the OS, and quit interception. | Validated | Architecture | Consistent with the Fastify-first architecture established in Epics 1–5 |
| A4 | Ad-hoc code signing (not Apple Developer ID signing) is sufficient for local installation on macOS. On Apple Silicon Macs running macOS Sequoia+, ad-hoc signing avoids the "app is damaged" error that has no GUI bypass. Users see the "unverified developer" Gatekeeper warning once, bypassable via System Settings > Privacy & Security. | Unvalidated | Tech Lead | electron-builder v26+ applies ad-hoc signatures to ARM64 builds automatically |
| A5 | Tab state persistence stores the list of open tab paths (and optionally scroll positions and mode) in the existing session JSON, not in Electron-specific storage. This keeps the persistence mechanism consistent with Epics 1–5 and means tab restore also works for the browser-based app. The base infrastructure already exists: `openTabs`/`activeTab` fields in `SessionState`, `PUT /api/session/tabs` endpoint, `syncTabsToSession()` on every tab open/close/switch, and `restoreTabsFromSession()` on startup. This epic extends the persisted shape to include per-tab mode. | Validated | Architecture | Infrastructure built during Epics 2–5; see `session.service.ts`, `schemas/index.ts` |
| A6 | Performance hardening thresholds (10K lines, 1K files, 20 tabs) are targets, not hard guarantees for every possible machine. The requirement is: the app remains usable (no freezing, no crash) at these scales on a modern Mac (M1+ or equivalent Intel). | Validated | Product | Exact performance numbers depend on hardware; the commitment is graceful behavior, not specific latency targets |
| A7 | The native menu bar in Electron replaces the web app's custom HTML menu bar. The same menu items and keyboard shortcuts are available in both modes, but the Electron version uses native OS menus. The web app's HTML menu bar remains functional for browser-based use. | Unvalidated | Tech Lead | See Tech Design Question 4 |

---

## Flows & Requirements

### 1. Large File Handling

The user opens a markdown document with 10,000+ lines. The document renders
without freezing the UI, scrolls smoothly, and can be edited in Edit mode
without typing lag.

#### Acceptance Criteria

**AC-1.1:** Documents over 10,000 lines render without freezing the UI

- **TC-1.1a: Large file render**
  - Given: A markdown file with 10,000 lines containing headings, paragraphs, code blocks, tables, and images
  - When: User opens the file
  - Then: The document renders and is visible within 5 seconds. The UI does not freeze during rendering — the loading indicator is animated throughout.
- **TC-1.1b: Large file scrolling**
  - Given: A 10,000-line document is rendered
  - When: User scrolls through the document
  - Then: Scrolling does not freeze the UI — the page responds to scroll input continuously without multi-second hangs
- **TC-1.1c: Large file with Mermaid**
  - Given: A 10,000-line document contains 5 Mermaid diagrams
  - When: User opens the file
  - Then: The document renders progressively — text content appears first, Mermaid diagrams render asynchronously without blocking the display of surrounding content

**AC-1.2:** Documents over 10,000 lines edit without typing lag

- **TC-1.2a: Large file edit mode**
  - Given: A 10,000-line document is open in Edit mode
  - When: User types
  - Then: Keystrokes appear without perceptible lag (under 50ms)
- **TC-1.2b: Large file mode switch**
  - Given: A 10,000-line document is open
  - When: User switches between Render and Edit modes
  - Then: Mode switch completes within 3 seconds. A loading indicator appears if the switch takes more than 500ms.

### 2. Deep File Tree Handling

The user points the app at a directory containing 1,000+ markdown files across
deeply nested subdirectories. The tree loads, expands, and responds to
interactions without freezing.

#### Acceptance Criteria

**AC-2.1:** File trees with 1,000+ markdown files load without freezing the UI

- **TC-2.1a: Large tree initial load**
  - Given: A root directory contains 1,500 markdown files across 200 directories, some nested 10+ levels deep
  - When: User sets this as the root
  - Then: The file tree populates within 5 seconds. A loading indicator appears during the scan. The UI remains responsive during loading.
- **TC-2.1b: Expand All on large tree**
  - Given: A tree with 1,000+ markdown files
  - When: User clicks Expand All
  - Then: The tree fully expands without freezing the UI. The expansion may be progressive (directories expand as they load) rather than all-at-once.
- **TC-2.1c: Scroll performance in expanded tree**
  - Given: A large tree is fully expanded, showing 1,000+ visible nodes
  - When: User scrolls through the tree
  - Then: Scrolling does not freeze the UI — the tree responds to scroll input continuously without multi-second hangs

**AC-2.2:** File tree markdown count calculation does not block the UI

- **TC-2.2a: Count badges on large tree**
  - Given: A root with 200 directories, each requiring recursive markdown count
  - When: Tree loads
  - Then: Count badges appear. If counts take time, directories appear first and counts fill in asynchronously.

### 3. Many-Tab Performance

The user has 20+ tabs open simultaneously. Tab switching, rendering, and
general app interaction remain responsive. Memory usage does not grow unboundedly.

#### Acceptance Criteria

**AC-3.1:** 20+ open tabs do not degrade switching or rendering performance

- **TC-3.1a: Tab switching with many tabs**
  - Given: 25 tabs are open, each with a different rendered document
  - When: User switches between tabs
  - Then: Tab switching is instant (under 200ms). The previously rendered content appears without re-rendering from scratch.
- **TC-3.1b: Opening additional tabs**
  - Given: 20 tabs are already open
  - When: User opens a 21st document
  - Then: The new tab opens and renders at the same speed as when few tabs were open
- **TC-3.1c: Tab strip with many tabs**
  - Given: 30 tabs are open
  - When: User views the tab strip
  - Then: The tab strip scrolls horizontally without lag. Tab count indicator shows "30 tabs".

**AC-3.2:** Memory usage is bounded with many open tabs

- **TC-3.2a: Memory does not grow unboundedly**
  - Given: User opens 25 tabs, then closes 20 of them
  - When: The closed tabs' resources are released
  - Then: Memory usage decreases after tab closure. Exact figures depend on document sizes, but closing tabs should reclaim most of their associated memory.
- **TC-3.2b: File watchers released**
  - Given: 25 tabs are open (25 file watchers active)
  - When: User closes 20 tabs
  - Then: 20 file watchers are released, leaving 5 active

### 4. Startup Optimization

The app is ready to use within a few seconds of launch. For the browser-based
app, this means the server starts and the page loads. For Electron, this means
the window appears with content.

#### Acceptance Criteria

**AC-4.1:** App is ready to use within 3 seconds of launch

- **TC-4.1a: Browser app startup**
  - Given: User runs the start command
  - When: The server starts and the browser page loads
  - Then: The app shell is visible and interactive within 3 seconds. Session state (workspaces, root, theme) is restored.
- **TC-4.1b: Electron app startup**
  - Given: User double-clicks the Electron app in ~/Applications
  - When: The app launches
  - Then: A window appears within 2 seconds (may show loading state). The app is fully interactive within 4 seconds. No white flash — the window is shown only after initial content is ready.
- **TC-4.1c: Startup with restored tabs**
  - Given: The user previously had 10 tabs open (tab persistence enabled)
  - When: App launches
  - Then: Tab strip shows the 10 tabs. The active tab's content loads first. Other tabs load lazily (content fetched when the user switches to them).

### 5. Filesystem Edge Cases

The app handles filesystem anomalies gracefully — no crashes, no silent
failures, no hanging.

#### Acceptance Criteria

**AC-5.1:** Permission-denied paths are handled gracefully

- **TC-5.1a: Unreadable file in tree**
  - Given: A root directory contains a `.md` file the user cannot read
  - When: File tree scans the directory
  - Then: The file appears in the tree (it has a `.md` extension). When clicked, an error is shown (consistent with Epic 2 AC-9.1a). The tree scan is not interrupted.
- **TC-5.1b: Unreadable subdirectory**
  - Given: A root directory contains a subdirectory the user cannot read
  - When: File tree scans the directory
  - Then: The subdirectory is skipped silently (its markdown count is 0). Other directories scan normally. No error toast for directory-level permission issues during tree scan — these are expected in shared filesystems.

**AC-5.2:** Symlink edge cases are handled without crashing

Note: Epic 1 AC-10.3 specifies basic symlink loop detection and special file handling. This AC hardens that behavior at scale — ensuring loop detection works in trees with thousands of entries and deep nesting, without stack overflow or unbounded memory.

- **TC-5.2a: Symlink loop**
  - Given: A directory contains a symlink that creates a loop (e.g., `a/b/c → a`)
  - When: File tree scans the directory
  - Then: The loop is detected and the symlink is skipped. The rest of the tree renders normally. No hang, no crash, no infinite recursion.
- **TC-5.2b: Broken symlink**
  - Given: A directory contains a symlink pointing to a nonexistent target
  - When: File tree scans the directory
  - Then: The broken symlink is excluded from the tree. No error — broken symlinks are common.
- **TC-5.2c: Symlink to file outside root**
  - Given: A symlink inside the root points to a `.md` file outside the root
  - When: File tree scans the directory
  - Then: The symlinked file appears in the tree with the symlink's path (inside the root), consistent with Epic 1 TC-5.1h. The tree never exposes paths outside the root.

**AC-5.3:** Network-mounted and slow filesystems do not hang the app

- **TC-5.3a: Slow tree scan**
  - Given: The root is on a network-mounted filesystem with high latency
  - When: User sets the root
  - Then: A loading indicator appears. The scan completes (possibly slowly) without freezing the UI. If the scan takes more than 10 seconds, a timeout message is shown with an option to retry.
- **TC-5.3b: Slow file read**
  - Given: A file is on a network-mounted filesystem
  - When: User opens the file
  - Then: The loading indicator appears. If the read takes more than 10 seconds, a timeout error is shown. The app remains responsive during the wait.
- **TC-5.3c: Filesystem disconnects mid-operation**
  - Given: A network filesystem disconnects while a file is being read or a tree is being scanned
  - When: The operation fails
  - Then: An error is shown. The app does not crash or hang. The user can switch to a different root or retry.

**AC-5.4:** Extremely deep directory nesting is handled

- **TC-5.4a: Deep nesting**
  - Given: A directory structure is nested 50+ levels deep with markdown files at the bottom
  - When: File tree scans the directory
  - Then: The tree renders correctly. Performance may degrade at extreme depths but the app does not crash or exceed call stack limits.

### 6. Mermaid Render Caching

Mermaid diagrams are cached after initial rendering. Re-displaying a cached
diagram (on tab switch, mode switch, or theme switch within the same theme)
does not re-invoke the Mermaid rendering library. The cache is bounded.

#### Acceptance Criteria

**AC-6.1:** Rendered Mermaid diagrams are cached

- **TC-6.1a: Tab switch cache hit**
  - Given: A document with 3 Mermaid diagrams is rendered. User switches to another tab and back.
  - When: The original tab is reactivated
  - Then: The Mermaid diagrams appear immediately from cache without re-rendering
- **TC-6.1b: Mode switch cache hit**
  - Given: A document with Mermaid diagrams is in Render mode. User switches to Edit, then back to Render.
  - When: Render mode is reactivated
  - Then: The Mermaid diagrams appear immediately from cache, provided the markdown source has not changed
- **TC-6.1c: Cache invalidation on content change**
  - Given: A Mermaid diagram is cached
  - When: The document's markdown is modified (edited or auto-reloaded from disk) such that the Mermaid block's source text changes
  - Then: The cached SVG is invalidated and the diagram re-renders from the new source

**AC-6.2:** Theme switch re-renders Mermaid diagrams with the new theme

- **TC-6.2a: Theme change invalidates cache**
  - Given: A document with cached Mermaid diagrams is displayed under a light theme
  - When: User switches to a dark theme
  - Then: Diagrams re-render with dark theme colors. The light-theme SVGs are no longer used.
- **TC-6.2b: Switch back to original theme**
  - Given: User switched from light to dark theme, then back to light
  - When: Light theme is reactivated
  - Then: If the light-theme SVGs are still in the cache, they are reused without re-rendering. If evicted, they re-render.

**AC-6.3:** The cache is bounded and does not grow unboundedly

- **TC-6.3a: Cache eviction**
  - Given: The cache has reached its size limit
  - When: A new Mermaid diagram needs to be cached
  - Then: The least recently used entry is evicted to make room. The evicted diagram will re-render if displayed again.
- **TC-6.3b: Cache cleared on tab close**
  - Given: A tab with cached Mermaid diagrams is closed
  - When: The tab is removed
  - Then: The cache entries associated with that tab are removed from the cache immediately

### 7. Electron Shell and Window Management

The Electron wrapper runs Fastify in-process and displays the web app in a
BrowserWindow. The shell handles app lifecycle, window management, and native
macOS integration.

#### Acceptance Criteria

**AC-7.1:** Electron runs Fastify in-process and displays the web app

- **TC-7.1a: In-process server startup**
  - Given: User launches the Electron app
  - When: The app starts
  - Then: Fastify starts in the Electron main process. A BrowserWindow loads `http://localhost:{port}`. The web app renders identically to the browser-based version.
- **TC-7.1b: Server port selection**
  - Given: The default port is in use
  - When: Electron starts the server
  - Then: The server binds to the next available port (consistent with Epic 1 AC-1.1 TC-1.1c). The BrowserWindow loads the correct URL.
- **TC-7.1c: No white flash on startup**
  - Given: The Electron app is launching
  - When: The BrowserWindow is created
  - Then: The window is not shown until initial content is ready. The user does not see a white or blank window.

**AC-7.2:** Single-instance lock prevents multiple app instances

- **TC-7.2a: Second launch with no file argument**
  - Given: The app is already running
  - When: User launches the app again
  - Then: The existing window is focused. No second instance is created.
- **TC-7.2b: Second launch with a file argument**
  - Given: The app is already running
  - When: User opens a `.md` file that triggers a second launch (e.g., `open` command or Finder double-click)
  - Then: The existing window is focused and the file opens in a new tab in the running instance.

**AC-7.3:** Window remembers size and position across restarts

- **TC-7.3a: Window state persisted**
  - Given: User resizes and moves the Electron window
  - When: App is quit and relaunched
  - Then: The window opens at the same size and position
- **TC-7.3b: Window on disconnected display**
  - Given: User used the app on an external monitor that is now disconnected
  - When: App launches
  - Then: The window opens on the primary display at a reasonable size rather than off-screen

### 8. Native Menu Bar

The Electron wrapper provides a native macOS menu bar that mirrors the web
app's menu structure. Menu items trigger the same actions via IPC to the
renderer.

#### Acceptance Criteria

**AC-8.1:** Native menu bar provides File, Export, and View menus matching the web app

- **TC-8.1a: File menu contents**
  - Given: The Electron app is running
  - When: User clicks the File menu in the native menu bar
  - Then: Options include: Open File (Cmd+O), Open Folder (Cmd+Shift+O), Save (Cmd+S), Save As (Cmd+Shift+S), Close Tab (Cmd+W). Keyboard shortcuts are shown. Items match the web app's File menu.
- **TC-8.1b: Export menu contents**
  - Given: A document is open
  - When: User clicks the Export menu
  - Then: Options include: PDF, DOCX, HTML, with keyboard shortcuts. Disabled when no document is open (consistent with the web app's Export behavior).
- **TC-8.1c: View menu contents**
  - Given: The Electron app is running
  - When: User clicks the View menu
  - Then: Options include: Toggle Sidebar (Cmd+\), theme submenu, and Render/Edit mode toggle. Matches the web app's View menu.
- **TC-8.1d: Standard macOS app menu**
  - Given: The Electron app is running
  - When: User clicks the app name menu (MD Viewer)
  - Then: Standard macOS items appear: About MD Viewer, Hide, Hide Others, Show All, Quit (Cmd+Q)
- **TC-8.1e: Menu item triggers web app action**
  - Given: User selects File > Open Folder from the native menu
  - When: The menu action fires
  - Then: The same folder picker dialog opens as when using the web app's File menu. The result is the same.

**AC-8.2:** Native menu items reflect current app state

- **TC-8.2a: Export disabled when no document open**
  - Given: No document tabs are open
  - When: User views the Export menu
  - Then: Export options are disabled (consistent with web app behavior)
- **TC-8.2b: Save enabled/disabled based on dirty state**
  - Given: The active tab has no unsaved changes
  - When: User views the File menu
  - Then: Save is disabled. Save As is enabled.
- **TC-8.2c: Save enabled when dirty**
  - Given: The active tab has unsaved changes
  - When: User views the File menu
  - Then: Save is enabled.
- **TC-8.2d: Theme checkmark reflects active theme**
  - Given: The user has selected a dark theme
  - When: User views the View > Theme submenu
  - Then: The active dark theme has a checkmark. Other themes do not.

**AC-8.3:** Web app's HTML menu bar is hidden in Electron

- **TC-8.3a: No duplicate menu bar**
  - Given: The app is running in Electron
  - When: User views the app window
  - Then: The web app's custom HTML menu bar is hidden. Only the native macOS menu bar is visible. All functionality remains accessible via the native menu.
- **TC-8.3b: Web app menu bar remains in browser**
  - Given: The same app is accessed via a browser (not Electron)
  - When: User views the app
  - Then: The web app's HTML menu bar is visible and functional (no regression)

### 9. File Associations and OS Integration

The Electron app registers as a handler for `.md` and `.markdown` files on
macOS. Users can double-click markdown files in Finder to open them in the
app.

#### Acceptance Criteria

**AC-9.1:** App registers as a `.md` / `.markdown` file handler

- **TC-9.1a: File association registered**
  - Given: The Electron app has been installed and launched at least once
  - When: User right-clicks a `.md` file in Finder and selects "Open With"
  - Then: MD Viewer appears in the list of available applications
- **TC-9.1b: Default handler**
  - Given: User sets MD Viewer as the default handler for `.md` files (via Finder "Open With > Always Open With")
  - When: User double-clicks a `.md` file
  - Then: The file opens in MD Viewer

**AC-9.2:** Opening a file from Finder opens it in the app

- **TC-9.2a: App not running — file opens on launch**
  - Given: The app is not running
  - When: User double-clicks a `.md` file in Finder
  - Then: The app launches and the file is open in a tab when the app is ready
- **TC-9.2b: App already running — file opens in existing window**
  - Given: The app is running with other documents open
  - When: User double-clicks a `.md` file in Finder
  - Then: The existing window is focused and the file opens in a new tab
- **TC-9.2c: Drag file onto dock icon**
  - Given: The app is running
  - When: User drags a `.md` file onto the dock icon
  - Then: The file opens in a new tab in the existing window
- **TC-9.2d: File already open**
  - Given: The file is already open in a tab
  - When: User double-clicks the same file in Finder
  - Then: The existing tab is activated (consistent with Epic 2 AC-1.3)
- **TC-9.2e: File open during startup with tab restore**
  - Given: User double-clicks a `.md` file while the app is not running, and the app has 5 persisted tabs from a previous session
  - When: The app launches
  - Then: The 5 persisted tabs are restored AND the double-clicked file opens in a new tab (or its existing tab is activated if it was among the persisted tabs). The double-clicked file becomes the active tab.

**AC-9.3:** Rebuilding the app preserves file associations

- **TC-9.3a: Stable bundle ID**
  - Given: The app was installed and file associations were set
  - When: The app is rebuilt and reinstalled at the same path with the same bundle ID
  - Then: File associations continue to work without the user needing to reconfigure

### 10. Electron Quit Flow

The Electron wrapper uses the custom quit modal specced in Epic 5 AC-5.3a–d
instead of the browser's generic `beforeunload` prompt.

#### Acceptance Criteria

**AC-10.1:** Quitting Electron with dirty tabs shows the custom modal

- **TC-10.1a: Quit via Cmd+Q with dirty tabs**
  - Given: 2 of 5 tabs have unsaved edits
  - When: User presses Cmd+Q
  - Then: A custom modal appears listing the 2 dirty files by name. Options: Save All and Quit, Discard All and Quit, Cancel.
- **TC-10.1b: Quit via window close button with dirty tabs**
  - Given: Tabs have unsaved edits
  - When: User clicks the window close button (red traffic light)
  - Then: The same custom modal appears
- **TC-10.1c: Save All and Quit**
  - Given: The quit modal is open with 2 dirty files
  - When: User clicks Save All and Quit
  - Then: Both files are saved. If both saves succeed, the app quits. If a save fails, the quit is aborted and the error is shown.
- **TC-10.1d: Discard All and Quit**
  - Given: The quit modal is open
  - When: User clicks Discard All and Quit
  - Then: The app quits without saving. Edits are lost.
- **TC-10.1e: Cancel quit**
  - Given: The quit modal is open
  - When: User clicks Cancel
  - Then: The modal closes. The app remains open with all tabs and edits intact.
- **TC-10.1f: Quit with no dirty tabs**
  - Given: All tabs are clean
  - When: User presses Cmd+Q or clicks the close button
  - Then: The app quits immediately with no prompt.

### 11. Tab State Persistence

Tab state (which files are open, which is active) persists across app
restarts. This was explicitly deferred from Epics 2 and 5.

#### Acceptance Criteria

**AC-11.1:** Open tabs are restored on app restart

- **TC-11.1a: Tabs restored**
  - Given: User had 5 tabs open with document A active
  - When: App is quit (clean — no dirty tabs) and relaunched
  - Then: The same 5 tabs appear in the tab strip. Document A is the active tab.
- **TC-11.1b: Active tab loads eagerly, others lazy**
  - Given: 10 tabs are restored
  - When: App launches
  - Then: The active tab's content loads and renders immediately. Other tabs show in the tab strip but their content loads only when the user switches to them.
- **TC-11.1c: Tab mode restored**
  - Given: Tab A was in Edit mode, Tab B was in Render mode
  - When: App restarts
  - Then: Tab A opens in Edit mode, Tab B in Render mode
- **TC-11.1d: Missing file on restore**
  - Given: A persisted tab points to a file that was deleted since the last session
  - When: App launches
  - Then: The tab appears with a "file not found" indicator (consistent with Epic 2 AC-7.3). Other tabs load normally.

**AC-11.2:** Dirty tabs are not persisted — the quit modal is the safety net

- **TC-11.2a: Quit with unsaved changes**
  - Given: 5 tabs are open, 2 of which have unsaved changes. The user confirms Discard All and Quit.
  - When: App relaunches
  - Then: All 5 tabs appear (including the 2 that were dirty). Content is loaded from disk — the unsaved edits are gone. No dirty indicators.
- **TC-11.2b: Save All and Quit persists saved state**
  - Given: 2 tabs had unsaved changes and the user chose Save All and Quit
  - When: App relaunches
  - Then: All tabs appear. The 2 previously-dirty files show their saved content.

**AC-11.3:** Tab list is persisted on every tab open and close, not only at quit

- **TC-11.3a: Crash recovery**
  - Given: User has 5 tabs open. The app crashes (or the browser tab is forcefully closed).
  - When: App relaunches
  - Then: The 5 tabs are restored. The crash did not lose the tab list because it was persisted incrementally, not just at quit time.
- **TC-11.3b: Browser tab close**
  - Given: User is running the browser-based app with 5 tabs open
  - When: User closes the browser tab (bypassing `beforeunload` or confirming the prompt)
  - Then: On next launch, the 5 tabs are restored because tab state was persisted on every tab open/close event.

### 12. Packaging and Installation

The Electron app is packaged for macOS with a stable bundle ID, ad-hoc code
signing, and a one-command install script.

#### Acceptance Criteria

**AC-12.1:** One-command install script builds and installs the app

- **TC-12.1a: Install script**
  - Given: User has Node.js installed and the repository cloned
  - When: User runs the install command (e.g., `npm run install-app`)
  - Then: The app is built, packaged, and placed at `~/Applications/MD Viewer.app` (or a stable user-writable path). No `sudo` required.
- **TC-12.1b: Reinstall preserves user data**
  - Given: The app is installed and user has workspaces, theme, and session state saved
  - When: User reinstalls (runs the install command again)
  - Then: User data (session state, workspaces, themes, persisted tabs) is preserved. Only the app binary is replaced.

**AC-12.2:** App has a stable bundle ID and ad-hoc code signature

- **TC-12.2a: Bundle ID**
  - Given: The app is packaged
  - When: The bundle metadata is inspected
  - Then: The bundle ID is stable across builds (e.g., `com.leemoore.mdviewer`). Reinstalling does not change the bundle ID.
- **TC-12.2b: Ad-hoc signature**
  - Given: The app is packaged on an Apple Silicon Mac
  - When: The signature is inspected (`codesign -v`)
  - Then: The app has an ad-hoc signature. It does not show "app is damaged" on launch. The user sees the "unverified developer" Gatekeeper prompt once.
- **TC-12.2c: Intel Mac compatibility**
  - Given: The app is packaged
  - When: Run on an Intel Mac
  - Then: The app launches and functions correctly. Ad-hoc signing is not required on Intel but does not cause issues.

**AC-12.3:** User data is stored in a stable, user-writable location

- **TC-12.3a: Data directory**
  - Given: The app is running (browser or Electron)
  - When: Session data, workspaces, and persisted tab state are written
  - Then: Data is stored in a consistent location (e.g., `~/.mdviewer/` or `~/Library/Application Support/MD Viewer/`) that survives app reinstallation
- **TC-12.3b: Browser and Electron share data**
  - Given: The user has used the browser-based app and then switches to Electron (or vice versa)
  - When: The app launches in the other mode
  - Then: Session state (workspaces, theme, recent files) is shared — both modes read from the same data directory

### 13. Error Handling

Hardening introduces new error scenarios from scale and edge cases. All errors
produce visible feedback.

#### Acceptance Criteria

**AC-13.1:** Performance-related errors produce visible feedback

- **TC-13.1a: File too large to render**
  - Given: A markdown file is exceptionally large (e.g., 50,000+ lines or 10MB+)
  - When: User opens the file
  - Then: The file opens with a loading indicator. Rendering completes (possibly slowly). The app does not crash or freeze permanently. No warning is required — the file renders as any other document.
- **TC-13.1b: Tree scan timeout**
  - Given: A root directory is extremely large or on a very slow filesystem
  - When: The tree scan exceeds a reasonable timeout
  - Then: A partial tree is shown (what was scanned so far) with an indicator that the scan was incomplete. The user can retry.

**AC-13.2:** Electron-specific errors are handled gracefully

- **TC-13.2a: Server fails to start in Electron**
  - Given: The Electron app is launching but Fastify fails to start (port conflict where all ports are exhausted, or other startup error)
  - When: The startup error occurs
  - Then: The window shows an error message explaining that the server could not start, rather than a blank window or crash.
- **TC-13.2b: Server crashes mid-session**
  - Given: The Fastify server crashes during normal use in Electron
  - When: API requests fail
  - Then: The web app shows server connection errors (consistent with Epic 2 AC-9.3). The Electron wrapper shows a "Server disconnected — Restart" button. Clicking it restarts the Fastify server and reloads the page.

---

## Data Contracts

### Session State Extension

The existing `SessionState` already has `openTabs: string[]` (ordered list of
absolute paths) and `activeTab: string | null`. This epic extends `openTabs`
from a flat path array to a structured array that includes per-tab mode:

```typescript
interface SessionState {
  // ... all existing Epic 1–5 fields ...
  openTabs: PersistedTab[];       // extended from string[] to include mode
  activeTab: string | null;       // unchanged — path of the active tab
}

interface PersistedTab {
  path: string;                   // absolute path to the file
  mode: "render" | "edit";        // which mode the tab was in
  scrollPosition?: number;        // approximate scroll offset (best-effort)
}
```

Note: The existing schema stores `openTabs` as `string[]`. The migration path
is straightforward — if a string is encountered instead of an object, treat it
as `{ path: string, mode: defaultOpenMode }`. The Zod schema should accept
both shapes during the transition.

### Mermaid Cache Entry

```typescript
interface MermaidCacheEntry {
  sourceHash: string;        // hash of the Mermaid source text
  themeId: string;           // theme ID at render time
  svg: string;               // rendered SVG markup
  accessedAt: number;        // timestamp for LRU eviction
}
```

Note: The cache is an internal implementation detail. It does not have an API
endpoint — it lives in the client's rendering pipeline. The interface above
documents the cache entry shape for the Tech Lead.

### Window State (Electron only)

```typescript
interface WindowState {
  x: number;
  y: number;
  width: number;
  height: number;
  maximized: boolean;
}
```

### IPC Channels (Electron only)

The Electron wrapper uses a minimal set of IPC channels. All file operations
continue to use the Fastify HTTP API — IPC is only for capabilities the browser
cannot provide.

| Channel | Direction | Purpose |
|---------|-----------|---------|
| `menu:action` | main → renderer | Native menu item selected (action name + args) |
| `app:open-file` | main → renderer | OS requested a file open (path) |
| `app:quit-request` | main → renderer | User initiated quit — renderer checks for dirty tabs |
| `app:quit-confirmed` | renderer → main | Renderer confirms quit is safe (all tabs saved/discarded) |
| `app:quit-cancelled` | renderer → main | Renderer cancelled the quit (user clicked Cancel) |
| `app:is-electron` | renderer → main | Renderer checks if running in Electron (for menu bar hiding, quit flow) |

### API Surface

No new Fastify API endpoints. Epic 6 uses existing endpoints. The only new
contracts are Electron IPC (above) and the session state extension.

Session-mutating endpoint affected:

| Method | Path | Change |
|--------|------|--------|
| GET | /api/session | Response already includes `openTabs` and `activeTab` — shape of `openTabs` entries changes from `string` to `PersistedTab` |
| PUT | /api/session/tabs | **Exists** — request shape changes from `{ openTabs: string[], activeTab: string \| null }` to `{ openTabs: PersistedTab[], activeTab: string \| null }` |

### Error Responses

No new API error codes. Performance timeouts and Electron startup failures are
handled at the client/wrapper level, not via new HTTP error codes.

---

## Dependencies

Technical dependencies:
- Epics 1–5 complete: full Fastify web app with browsing, viewing, rich content, export, and editing
- Electron (version confirmed in tech design)
- electron-builder (confirmed in tech design)
- The existing `startServer()` function in `app/src/server/index.ts` accepts injectable options — the Electron main process uses this to start Fastify in-process with `openUrl` set to a no-op

Process dependencies:
- Epic 5 tech design and implementation complete before Epic 6 implementation begins

---

## Non-Functional Requirements

### Performance
- 10,000-line documents render within 5 seconds and scroll smoothly
- File trees with 1,000+ markdown files load within 5 seconds
- Tab switching with 20+ tabs completes within 200ms
- App startup (server + initial page) completes within 3 seconds (browser) or 4 seconds (Electron)
- Mermaid cache hit eliminates re-rendering latency on tab switch and mode switch

### Memory
- Memory usage with 20+ open tabs stays under 500MB (exclusive of Puppeteer — the export engine is not counted as it only runs during exports)
- Closing tabs reclaims memory proportional to their content
- Mermaid cache is bounded (max entries/size determined in tech design)

### Reliability
- Symlink loops, broken symlinks, and permission errors do not crash the app or hang the tree scan
- Network filesystem timeouts produce visible errors within 10 seconds
- Electron server crash is recoverable (restart or clear error message)
- Tab state persistence uses the same atomic write pattern as existing session persistence

### Security
- Electron BrowserWindow uses context isolation, disables node integration, and enables sandbox
- The Electron wrapper loads content from `http://localhost:{port}` only — no remote URLs
- IPC surface is minimal — only the channels listed in Data Contracts
- File associations do not grant the app access to files beyond what the user explicitly opens

---

## Tech Design Questions

Questions for the Tech Lead to address during design:

1. **Electron version and build tooling:** Which Electron version? Confirm electron-builder (not electron-forge) for packaging. What are the minimum Node version requirements and how do they interact with the Fastify server's Node requirements?
2. **Tab state persistence — schema migration:** The existing `openTabs: string[]` needs to become `openTabs: PersistedTab[]` to store per-tab mode. How should the Zod schema handle the migration? Recommended: accept a union of `string` and `PersistedTab` per element, normalizing strings to `{ path, mode: defaultOpenMode }` on load.
3. **Mermaid cache implementation:** Should the cache use source-text hashing (e.g., SHA-256) or exact string comparison? What max size/entry count? Should the cache be per-tab or global across all tabs? Memory vs. performance trade-off.
4. **Native menu bar detection:** How should the web app detect that it's running in Electron to hide the HTML menu bar? Options: a query parameter from the Electron BrowserWindow URL, a custom header on the Fastify response, the `app:is-electron` IPC check, or `navigator.userAgent` parsing. The detection must work on initial page load to avoid a flash of the HTML menu bar.
5. **Native menu state synchronization:** The native menu needs to reflect app state (e.g., Export disabled when no document is open, Save disabled when not dirty, theme checkmark on active theme). How should the renderer communicate state changes to the main process to update the native menu? Options: IPC events on state change, periodic polling from main process, or the main process reads server state directly.
6. **Large file rendering strategy:** For 10,000+ line documents, should the rendering pipeline use virtualization (only render visible content), chunked rendering (render in batches with yields to the event loop), or rely on browser-native performance with optimized HTML output? CodeMirror 6 already uses virtualization for editing — what about the rendered view?
7. **File tree virtualization:** For 1,000+ node trees, should the sidebar use virtual scrolling (only render visible tree nodes in the DOM), or is DOM-based rendering fast enough? The POC rendered the full tree — will this scale?
8. **Electron preload script scope:** How much logic goes in the preload script vs. the main process? The minimal approach: preload exposes only `isElectron`, `onMenuAction`, `onOpenFile`, `requestQuit`, `confirmQuit`, `cancelQuit`. All file operations stay on HTTP.
9. **Electron app user data location:** Electron has `app.getPath('userData')` (typically `~/Library/Application Support/MD Viewer/`). Should session data move there, or stay in the current location? If the current location is `~/.mdviewer/`, how is data shared between browser and Electron modes?
10. **Install script implementation:** Should the install script use `npm run build && electron-builder` directly, or should there be a separate packaging script? Should it also create a symlink or shell script for CLI launch (`mdv` command)?

---

## Recommended Story Breakdown

### Story 0: Foundation (Infrastructure)

Types, test fixtures, and infrastructure needed by all Epic 6 stories.

- `PersistedTab` type definition (extends existing `openTabs` from `string[]` to `PersistedTab[]`)
- `SessionState` schema migration: `openTabs` accepts both `string[]` (legacy) and `PersistedTab[]` (new), `activeTab` field unchanged
- `MermaidCacheEntry` type definition (internal)
- `WindowState` type definition (Electron)
- Test fixtures: large markdown files (10K lines), large directory trees (1K+ files with symlinks, permissions edge cases, deep nesting)
- Existing `PUT /api/session/tabs` endpoint updated to accept new `PersistedTab` shape (no new endpoint needed)

### Story 1: Large File and Rendering Performance

**Delivers:** Documents over 10,000 lines render, scroll, and edit without freezing the UI. Mode switching on large files is responsive.
**Prerequisite:** Story 0, Epics 1–5 complete
**ACs covered:**
- AC-1.1 (large file rendering and scrolling)
- AC-1.2 (large file editing)

**Estimated test count:** 6 tests

### Story 2: File Tree and Startup Performance

**Delivers:** File trees with 1,000+ files load and interact smoothly. App startup is within target. Filesystem edge cases are handled gracefully.
**Prerequisite:** Story 0
**ACs covered:**
- AC-2.1 (large tree loading and interaction)
- AC-2.2 (async count badges)
- AC-4.1 (startup optimization — browser portion)
- AC-5.1 (permission-denied paths)
- AC-5.2 (symlink edge cases)
- AC-5.3 (network/slow filesystem)
- AC-5.4 (deep nesting)
- AC-13.1 (performance error feedback)

**Estimated test count:** 16 tests

### Story 3: Many-Tab Performance and Mermaid Caching

**Delivers:** 20+ open tabs do not degrade performance. Mermaid diagrams are cached to avoid redundant re-rendering.
**Prerequisite:** Story 0
**ACs covered:**
- AC-3.1 (many-tab switching and rendering)
- AC-3.2 (bounded memory with many tabs)
- AC-6.1 (Mermaid cache hits)
- AC-6.2 (theme switch cache behavior)
- AC-6.3 (bounded cache with eviction)

**Estimated test count:** 12 tests

### Story 4: Tab State Persistence

**Delivers:** Open tabs, active tab, and per-tab mode persist across app restarts. Lazy loading on restore.
**Prerequisite:** Story 0
**ACs covered:**
- AC-11.1 (tab restore on restart)
- AC-11.2 (dirty tabs not persisted)
- AC-11.3 (incremental tab persistence for crash recovery)
- AC-4.1c (startup with restored tabs)

**Estimated test count:** 8 tests

### Story 5: Electron Shell and Window Management

**Delivers:** Electron wrapper runs Fastify in-process, displays the web app, with single-instance lock, window state persistence, and no-flash startup.
**Prerequisite:** Story 4 (tab persistence should be in place before Electron wraps the app)
**ACs covered:**
- AC-7.1 (Electron runs Fastify in-process)
- AC-7.2 (single-instance lock)
- AC-7.3 (window state persistence)
- AC-4.1b (Electron startup time)
- AC-13.2 (Electron error handling)

**Estimated test count:** 10 tests

### Story 6: Native Menu Bar and Electron Quit Flow

**Delivers:** Native macOS menu bar mirrors web app menus. HTML menu bar hidden in Electron. Custom quit modal for dirty tabs.
**Prerequisite:** Story 5
**ACs covered:**
- AC-8.1 (native menu bar contents and actions)
- AC-8.2 (native menu state sync — disabled items, checkmarks)
- AC-8.3 (HTML menu bar hidden in Electron)
- AC-10.1 (Electron quit flow with dirty tabs modal)

**Estimated test count:** 10 tests

### Story 7: File Associations, Packaging, and Install

**Delivers:** App registers as `.md` handler. `open-file` events open documents. Packaging with stable bundle ID, ad-hoc signing, and install script.
**Prerequisite:** Story 5
**ACs covered:**
- AC-9.1 (file association registration)
- AC-9.2 (open from Finder/dock)
- AC-9.3 (stable bundle ID)
- AC-12.1 (install script)
- AC-12.2 (bundle ID and ad-hoc signing)
- AC-12.3 (user data location)

**Estimated test count:** 10 tests

---

## Amendments

### Amendment 1: Ad-hoc signing vs. code signing scope (2026-03-21)

**Changed:** Out of Scope clarification, AC-12.2b
**Reason:** The PRD excludes "Code signing and notarization." This refers to Apple Developer ID signing and Apple notarization — the processes required for public distribution and suppressing all Gatekeeper warnings. Ad-hoc signing is a different concern: it is a local, zero-cost, no-account build step that electron-builder applies automatically to ARM64 binaries. Without ad-hoc signing, macOS Sequoia on Apple Silicon shows "app is damaged" with no GUI bypass. Ad-hoc signing produces the "unverified developer" warning instead, which users can bypass via System Settings. This epic requires ad-hoc signing (a build-tool default) and continues to exclude Developer ID signing and notarization.

### Amendment 2: Tab persistence scope vs. PRD (2026-03-21)

**Changed:** In Scope addition of tab state persistence
**Reason:** The PRD does not explicitly list tab persistence in Feature 6. However, Epic 2 (A5) and Epic 5 (A6) both explicitly defer tab session restore to Epic 6. The infrastructure was built incrementally during Epics 2–5 (`openTabs`/`activeTab` in `SessionState`, `syncTabsToSession()`, `restoreTabsFromSession()`). This epic extends the persisted shape to include per-tab mode and specifies the restore behavior on launch.

### Amendment 3: Native menu bar replacing HTML menu bar (2026-03-21)

**Changed:** In Scope addition of HTML menu bar hiding in Electron
**Reason:** The PRD does not mention native menus or HTML menu bar hiding. This is a natural consequence of the Electron wrapper: macOS conventions expect native menus, and showing both native and HTML menus would be confusing. The HTML menu bar remains functional for browser-based use.

---

## Validation Checklist

- [x] User Profile has all four fields + Feature Overview
- [x] Flows cover all paths (happy, alternate, cancel/error)
- [x] Every AC is testable (no vague terms)
- [x] Every AC has at least one TC
- [x] TCs cover happy path, edge cases, and errors
- [x] Data contracts are fully typed
- [x] Scope boundaries are explicit (in/out/assumptions)
- [x] Story breakdown covers all ACs
- [x] Stories sequence logically (hardening → persistence → Electron shell → menus/quit → packaging)
- [x] All dependencies on Epics 1–5 are explicit
- [x] Epic 5 quit flow (AC-5.3a–d) activation documented
- [x] Epic 2 tab state deferral (A5) resolved
- [x] Electron architecture does not duplicate server logic (A3)
- [x] Ad-hoc signing requirement documented (A4)
- [x] Tab persistence available to both browser and Electron (A5)
- [x] Incremental tab persistence for crash recovery (AC-11.3)
- [x] Native menu bar / HTML menu bar coexistence specified (A7)
- [x] Performance thresholds qualified as targets, not hard guarantees (A6)
- [x] IPC surface is minimal — file operations stay on HTTP (A3)
- [x] Ad-hoc vs Developer ID signing distinction documented (Amendment 1)
- [x] Tab persistence scope documented as PRD deviation (Amendment 2)
- [x] Native menu bar scope documented as PRD deviation (Amendment 3)
- [x] Native menu state sync specified (AC-8.2)
- [x] Tab restore + open-file startup ordering specified (TC-9.2e)
- [x] Vague TCs tightened to binary/measurable outcomes
- [x] AC numbering sequential within each flow
- [x] Self-review complete
- [x] Verification round 1 complete (Codex GPT-5.4)
- [x] Critical and major issues from verification round 1 addressed
- [ ] Validation rounds complete (pending re-review)
